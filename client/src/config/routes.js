const routes = {
    login: '/',
    messages: '/messages',
    register: '/register',
    explore: '/explore',
    settings: '/settings',
    albums: '*',
    tagged: 'tagged',
    home: '/',
    profile: '/:username/*',
};

export default routes;
